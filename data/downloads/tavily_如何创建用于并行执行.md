* [设置](#setup)

1. [LangGraph](../..)
2. [指南](../)
3. [操作指南](../)
4. [LangGraph](../../how-tos#langgraph)
5. [可控性](../../how-tos#controllability)

# 如何为并行执行创建 Map-Reduce 分支[¶](#how-to-create-map-reduce-branches-for-parallel-execution "Permanent link")

[Map-reduce](https://en.wikipedia.org/wiki/MapReduce) 操作对于高效的任务分解和并行处理至关重要。这种方法包括将任务分解为更小的子任务，并行处理每个子任务，并聚合所有已完成子任务的结果。

考虑以下示例：给定用户提供的一个通用主题，生成相关主题列表，为每个主题生成一个笑话，并从结果列表中选择最佳笑话。在此设计模式中，第一个节点可能会生成一个对象列表（例如，相关主题），我们希望将其他节点（例如，生成笑话）应用于所有这些对象（例如，主题）。然而，出现了两个主要挑战。

(1) 在我们布局图时，对象的数量（例如，主题）可能事先未知（这意味着边的数量可能未知），并且 (2) 传递给下游节点的输入状态应该不同（每个生成的对象一个）。

LangGraph [通过其 `Send` API](/langgraphjs/concepts/low_level/#send) 解决了这些挑战。通过利用条件边，`Send` 可以将不同的状态（例如，主题）分发到节点的多个实例（例如，笑话生成）。重要的是，发送的状态可以与核心图的状态不同，从而实现灵活和动态的工作流管理。

## 设置[¶](#setup "永久链接")

此示例需要一些依赖项。首先，安装 LangGraph 库以及 `@langchain/anthropic` 包，因为我们将在本示例中使用 Anthropic LLM。

```
npm install @langchain/langgraph @langchain/anthropic @langchain/core     
```

接下来，设置您的 Anthropic API 密钥

```
process.env.ANTHROPIC_API_KEY = 'YOUR_API_KEY' process. env. ANTHROPIC_API_KEY  =  'YOUR_API_KEY'
```

```
import { z } from "zod"; import  { z  }  from  "zod";import { ChatAnthropic } from "@langchain/anthropic"; import  { ChatAnthropic  }  from  "@langchain/anthropic";import { StateGraph, END, START, Annotation, Send } from "@langchain/langgraph"; import  { StateGraph,  END,  START,  Annotation,  Send  }  from  "@langchain/langgraph";  /* Model and prompts */ /* Model and prompts */  // Define model and prompts we will use // Define model and prompts we will useconst subjectsPrompt = "Generate a comma separated list of between 2 and 5 examples related to: {topic}." const  subjectsPrompt  =  "Generate a comma separated list of between 2 and 5 examples related to: {topic}."const jokePrompt = "Generate a joke about {subject}" const  jokePrompt  =  "Generate a joke about {subject}"const bestJokePrompt = `Below are a bunch of jokes about {topic}. Select the best one! Return the ID (index) of the best one. const  bestJokePrompt  =  `Below are a bunch of jokes about {topic}. Select the best one! Return the ID (index) of the best one.  {jokes}` {jokes}`  // Zod schemas for getting structured output from the LLM // Zod schemas for getting structured output from the LLMconst Subjects = z.object({ const  Subjects  =  z. object({ subjects: z.array(z.string()),  subjects:  z.array(z. string()),}); });const Joke = z.object({ const  Joke  =  z. object({ joke: z.string(),  joke:  z.string(),}); });const BestJoke = z.object({ const  BestJoke  =  z. object({ id: z.number(),  id:  z.number(),}); });  const model = new ChatAnthropic({ const  model  =  new  ChatAnthropic({ model: "claude-3-5-sonnet-20240620",  model:  "claude-3-5-sonnet-20240620",}); });  /* Graph components: define the components that will make up the graph */ /* Graph components: define the components that will make up the graph */  // This will be the overall state of the main graph. // This will be the overall state of the main graph.// It will contain a topic (which we expect the user to provide) // It will contain a topic (which we expect the user to provide)// and then will generate a list of subjects, and then a joke for // and then will generate a list of subjects, and then a joke for// each subject // each subjectconst OverallState = Annotation.Root({ const  OverallState  =  Annotation. Root({ topic: Annotation<string>,  topic:  Annotation< string>, subjects: Annotation<string[]>,  subjects:  Annotation< string[]>, // Notice here we pass a reducer function.  // Notice here we pass a reducer function. // This is because we want combine all the jokes we generate  // This is because we want combine all the jokes we generate // from individual nodes back into one list.  // from individual nodes back into one list. jokes: Annotation<string[]>({  jokes:  Annotation< string[]>({ reducer: (state, update) => state.concat(update),  reducer:  (state,  update)  =>  state. concat(update), }),  }), bestSelectedJoke: Annotation<string>,  bestSelectedJoke:  Annotation< string>,}); });  // This will be the state of the node that we will "map" all // This will be the state of the node that we will "map" all// subjects to in order to generate a joke // subjects to in order to generate a jokeinterface JokeState { interface  JokeState  { subject: string;  subject:  string;} }  // This is the function we will use to generate the subjects of the jokes // This is the function we will use to generate the subjects of the jokesconst generateTopics = async ( const  generateTopics  =  async  ( state: typeof OverallState.State  state:  typeof  OverallState. State): Promise<Partial<typeof OverallState.State>> => { ):  Promise< Partial< typeof  OverallState. State>>  =>  { const prompt = subjectsPrompt.replace("topic", state.topic);  const  prompt  =  subjectsPrompt. replace("topic",  state. topic); const response = await model  const  response  =  await  model .withStructuredOutput(Subjects, { name: "subjects" })  . withStructuredOutput(Subjects,  { name:  "subjects"  }) .invoke(prompt);  . invoke(prompt); return { subjects: response.subjects };  return  { subjects:  response.subjects  };}; };  // Function to generate a joke // Function to generate a jokeconst generateJoke = async (state: JokeState): Promise<{ jokes: string[] }> => { const  generateJoke  =  async  (state:  JokeState):  Promise<{ jokes:  string[]  }>  =>  { const prompt = jokePrompt.replace("subject", state.subject);  const  prompt  =  jokePrompt. replace("subject",  state. subject); const response = await model  const  response  =  await  model .withStructuredOutput(Joke, { name: "joke" })  . withStructuredOutput(Joke,  { name:  "joke"  }) .invoke(prompt);  . invoke(prompt); return { jokes: [response.joke] };  return  { jokes:  [response. joke]  };}; };  // Here we define the logic to map out over the generated subjects // Here we define the logic to map out over the generated subjects// We will use this an edge in the graph // We will use this an edge in the graphconst continueToJokes = (state: typeof OverallState.State) => { const  continueToJokes  =  (state:  typeof  OverallState. State)  =>  { // We will return a list of `Send` objects  // We will return a list of `Send` objects // Each `Send` object consists of the name of a node in the graph  // Each `Send` object consists of the name of a node in the graph // as well as the state to send to that node  // as well as the state to send to that node return state.subjects.map((subject) => new Send("generateJoke", { subject }));  return  state. subjects. map((subject)  =>  new  Send("generateJoke",  { subject  }));}; };  // Here we will judge the best joke // Here we will judge the best jokeconst bestJoke = async ( const  bestJoke  =  async  ( state: typeof OverallState.State  state:  typeof  OverallState. State): Promise<Partial<typeof OverallState.State>> => { ):  Promise< Partial< typeof  OverallState. State>>  =>  { const jokes = state.jokes.join("\n\n");  const  jokes  =  state. jokes. join("\n\n"); const prompt = bestJokePrompt  const  prompt  =  bestJokePrompt .replace("jokes", jokes)  . replace("jokes",  jokes) .replace("topic", state.topic);  . replace("topic",  state. topic); const response = await model  const  response  =  await  model .withStructuredOutput(BestJoke, { name: "best_joke" })  . withStructuredOutput(BestJoke,  { name:  "best_joke"  }) .invoke(prompt);  . invoke(prompt); return { bestSelectedJoke: state.jokes[response.id] };  return  { bestSelectedJoke:  state.jokes[response. id]  };}; };  // Construct the graph: here we put everything together to construct our graph // Construct the graph: here we put everything together to construct our graphconst graph = new StateGraph(OverallState) const  graph  =  new  StateGraph(OverallState) .addNode("generateTopics", generateTopics)  . addNode("generateTopics",  generateTopics) .addNode("generateJoke", generateJoke)  . addNode("generateJoke",  generateJoke) .addNode("bestJoke", bestJoke)  . addNode("bestJoke",  bestJoke) .addEdge(START, "generateTopics")  . addEdge(START,  "generateTopics") .addConditionalEdges("generateTopics", continueToJokes)  . addConditionalEdges("generateTopics",  continueToJokes) .addEdge("generateJoke", "bestJoke")  . addEdge("generateJoke",  "bestJoke") .addEdge("bestJoke", END);  . addEdge("bestJoke",  END);  const app = graph.compile(); const  app  =  graph. compile();
```

```
import * as tslab from "tslab"; import  *  as  tslab  from  "tslab";  const representation = app.getGraph(); const  representation  =  app. getGraph();const image = await representation.drawMermaidPng(); const  image  =  await  representation. drawMermaidPng();const arrayBuffer = await image.arrayBuffer(); const  arrayBuffer  =  await  image. arrayBuffer();  tslab.display.png(new Uint8Array(arrayBuffer)); tslab. display. png(new  Uint8Array(arrayBuffer));
```

```
// Call the graph: here we call it to generate a list of jokes // Call the graph: here we call it to generate a list of jokesfor await (const s of await app.stream({ topic: "animals" })) { for  await  (const  s  of  await  app. stream({ topic:  "animals"  }))  { console.log(s);  console. log(s);} }
```

```
{ { generateTopics: { subjects: [ 'lion', 'elephant', 'penguin', 'dolphin' ] }  generateTopics: { subjects: [ 'lion', 'elephant', 'penguin', 'dolphin' ] }} }{ { generateJoke: {  generateJoke: { jokes: [ "Why don't lions like fast food? Because they can't catch it!" ]  jokes: [ "Why don't lions like fast food? Because they can't catch it!" ] }  }} }{ { generateJoke: {  generateJoke: { jokes: [  jokes: [ "Why don't elephants use computers? Because they're afraid of the mouse!"  "Why don't elephants use computers? Because they're afraid of the mouse!" ]  ] }  }} }{ { generateJoke: {  generateJoke: { jokes: [  jokes: [ "Why don't dolphins use smartphones? They're afraid of phishing!"  "Why don't dolphins use smartphones? They're afraid of phishing!" ]  ] }  }} }{ { generateJoke: {  generateJoke: { jokes: [  jokes: [ "Why don't you see penguins in Britain? Because they're afraid of Wales!"  "Why don't you see penguins in Britain? Because they're afraid of Wales!" ]  ] }  }} }{ { bestJoke: {  bestJoke: { bestSelectedJoke: "Why don't elephants use computers? Because they're afraid of the mouse!"  bestSelectedJoke: "Why don't elephants use computers? Because they're afraid of the mouse!" }  }} }
```