## 1. 去重工具与输入准备

- [x] 1.1 梳理 `collector_first`/`collector_second` 当前输入与输出字段，确认可用于去重的文档列表与文件路径来源
- [x] 1.2 在 `FileDeduplicator` 或新辅助函数中新增“仅输出非重复结果”的去重入口（基于文件列表，不做物理删除）

## 2. 第一阶段去重实现

- [x] 2.1 在 `collector_first` 中基于 `execute_first` 的文档集合调用新的去重入口，返回非重复结果列表
- [x] 2.2 保留重复记录的日志/统计（仅记录未输出条目，不删除文件）

## 3. 第二阶段去重实现

- [x] 3.1 在 `collector_second` 中先对 `execute_second` 集合做内部去重，输出非重复结果
- [x] 3.2 将第二阶段去重结果与第一阶段保留结果做跨阶段比对，重复时仅输出第一阶段结果
- [x] 3.3 更新返回的元数据字段与日志，确保跨阶段去重统计清晰且不包含物理删除

## 4. 验证与回归

- [ ] 4.1 使用本地样例或最小输入验证两阶段去重规则（第一阶段仅保留非重复，第二阶段让位于第一阶段）
- [ ] 4.2 确认未触发文件物理删除路径（`remove_duplicates=False` 或等价逻辑）
