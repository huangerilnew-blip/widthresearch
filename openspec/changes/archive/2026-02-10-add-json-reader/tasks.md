## 1. 结构与校验

- [x] 1.1 在 `core/rag/models.py` 中新增 `jsonreader` 类与方法签名
- [x] 1.2 实现 `.json` 扩展名校验与明确异常信息

## 2. 文件读取逻辑

- [x] 2.1 以 `utf-8` 按行读取文件并输出 `list[str]`（去除换行符）
- [x] 2.2 处理文件不存在与读取失败的异常路径

## 3. 验证与回归

- [x] 3.1 添加或更新最小化测试/示例（若仓库已有相关测试入口）
- [ ] 3.2 运行相关检查或测试命令并确认无新增报错
